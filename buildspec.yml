version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - $(aws ecr get-login --no-include-email --region $AWS_REGION)
      - echo Pulling latest build for cache...
      - docker pull $IMAGE_REPO:stable || true
  build:
    commands:
      - echo Build started on `date`, building the docker image...
      - docker build --cache-from $IMAGE_REPO:stable --tag agent:stable --build-arg stable agent
  post_build:
    commands:
      - echo Build completed on `date`, pushing the Docker image...
      - docker tag agent:stable $IMAGE_REPO:stable
      - docker push $IMAGE_REPO:stable
